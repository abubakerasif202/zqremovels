# Branding assets

`ab-digital-solutions-watermark.webp` — official AB Digital Solutions logo used by the
footer developer credit (`site-src/data/agency-credit.mjs`).

Current asset: 672x309 WebP, copied from the supplied official artwork and
rendered at `clamp(140px, 15vw, 170px)` wide. The original proportions and gold
artwork are preserved without recolouring or cropping.

Replace only from the supplied official artwork; do not redraw or stretch it.
